/*#include "Drone.hh"

 void Drone::Anim_Rotation(double angle, double frames,const std::string bodyName,const std::string lName,const std::string rName)
{
    this->body.Anim_Rotation(angle, frames,bodyName);
    lRotor.Anim_Rotation(angle,frames,lName);
    rRotor.Anim_Rotation(angle,frames,rName);
}
void Drone::Anim_Move(Vector3D translation,double velocity, double frames,const std::string bodyName,const std::string lName,const std::string rName)
{
    body.Anim_Move(translation,frames,bodyName);
    lRotor.Anim_Move(translation,velocity,frames,lName);
    rRotor.Anim_Move(translation,velocity,frames,rName);   
}
void Drone::draw(std::string bodyName, std::string lName, std::string rName) const
{
    body.draw(bodyName);
    lRotor.draw(lName);
    rRotor.draw(rName);
} */